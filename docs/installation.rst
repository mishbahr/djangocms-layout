============
Installation
============

At the command line::

    $ easy_install djangocms-layout

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv djangocms-layout
    $ pip install djangocms-layout